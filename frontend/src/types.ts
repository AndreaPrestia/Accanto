// Shim: i tipi del dominio sono ora condivisi tra il PWA web e l'app React Native.
// Tutti gli import esistenti (`from '../types'`) continuano a funzionare grazie al re-export.
export * from '@accanto/shared/types';
