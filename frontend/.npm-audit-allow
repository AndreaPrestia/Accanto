# Allowlist per scripts/ci/npm-audit-check.sh — vulnerabilita' tollerate
# perche' NON ESPLOITABILI nel nostro contesto.
# Formato: <GHSA-id> # <motivo> + <scadenza> + <owner>

# React Router "RSC Mode CSRF Bypass" — FALSO POSITIVO sulla nostra versione.
# L'advisory copre react-router 7.12.0–8.2.0; noi usiamo la 7.18.1 che e'
# PATCHED (npm propone erroneamente un downgrade a 7.11.0 come "fix", che
# rientrerebbe nel range vulnerabile). Inoltre: (1) la SPA usa react-router
# in modalita' client-side data router, NON React Server Components (la vuln
# e' specifica di RSC mode); (2) non c'e' nessun "action" server invocabile.
# Rivalutare al prossimo bump di react-router.
# Scadenza tolleranza: 2026-12-31. Owner: andrea.
GHSA-qwww-vcr4-c8h2 # react-router 7.18.1 patched + SPA client-side (no RSC), falso positivo, rivalutare al bump
