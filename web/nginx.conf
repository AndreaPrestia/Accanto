server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Nasconde la versione nginx nell'header Server.
    server_tokens off;

    # Asset hashati di Astro: cache lunga
    location /_astro/ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
        include /etc/nginx/conf.d/security-headers.conf;
    }

    # Sitemap e robots: niente cache aggressiva
    location = /robots.txt {
        try_files $uri =404;
        add_header Cache-Control "no-cache";
        include /etc/nginx/conf.d/security-headers.conf;
    }
    location ~ ^/sitemap.*\.xml$ {
        try_files $uri =404;
        add_header Cache-Control "no-cache";
        include /etc/nginx/conf.d/security-headers.conf;
    }

    # RFC 8615: .well-known/ (security.txt et al.). Servito come text/plain
    # senza cache aggressiva — i client lo rileggono regolarmente.
    location ^~ /.well-known/ {
        default_type text/plain;
        try_files $uri =404;
        add_header Cache-Control "no-cache";
        include /etc/nginx/conf.d/security-headers.conf;
    }

    # Pagine: Astro emette /<locale>/<slug>/index.html con trailingSlash:'never'.
    # try_files prova URL letterale, .html, /index.html, poi 404.
    location / {
        try_files $uri $uri.html $uri/index.html =404;
        add_header Cache-Control "no-cache";
        include /etc/nginx/conf.d/security-headers.conf;
    }
}
