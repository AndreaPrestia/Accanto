import PlaceholderScreen from './_PlaceholderScreen';

export default function SharedUpdatesScreen() {
  return <PlaceholderScreen name="SharedUpdates" subtitle="Implementazione in arrivo (Phase 5)." />;
}
